package spec;

public interface mode {
	/**
	 * Find one of repeated integer in array
	 * @param an array which contains more than one value and repeation of values
	 * @return a number that is repeated  
	 */
	public int getmode(int[ ] val);
}
